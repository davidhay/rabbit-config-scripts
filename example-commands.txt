# assumes that RBT_PASSWD is set
./delete-all-queues.sh -u rabbit -p $RBT_PASSWD -v vh1

./show-all-per-vhost.sh -u rabbit -p $RBT_PASSWD

rabbitmqadmin --vhost=vh1  --user=rabbit --password=$RBT_PASSWD list bindings

===
WORKING [VIA SCRIPTS - needs access to rabbitmqadmin]
./show-all-for-vhost.sh -u rabbit -p $RBT_PASSWD -v vh1
./show-all-vhosts.sh -u rabbit -p $RBT_PASSWD
./delete-resources-for-vhost.sh -u rabbit -p $RBT_PASSWD -v vh1
./import-broker-definitions-for-vhost.sh -u rabbit -p $RBT_PASSWD -v vh1 -f ./broker-definitions.json

this could run anywhere that has access to node and port.
===
docker exec 8078697073ff /bin/bash -c "rabbitmqctl list_users"
docker exec 8078697073ff /bin/bash -c "rabbitmqctl list_vhosts"
===
